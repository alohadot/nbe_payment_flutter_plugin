org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Required by nbe_payment_flutter_plugin: the bundled 3DS SDK still references the legacy
# com.android.support libraries, which must be rewritten to AndroidX at build time.
android.enableJetifier=true
